# 无人机返航 V0.1 插件开发、编译、部署与 AI 交接工作流

> 文档版本：V1.2  
> 最近更新：2026-08-27  
> 适用范围：历史轨迹原路返航 V0.1、`manual_return_v01.so`、老板提供的 `px4ctrl_release` 二进制包  
> 核心原则：算法先在 `~/fanhang` 验证，再把可由插件 API 表达的部分同步到插件源码，最后必须使用老板指定的 Docker 镜像重新编译 `.so`。

## 1. 这份文档解决什么问题

本项目目前同时存在两套相关代码：

1. `~/fanhang`：算法开发、ROS/RViz 仿真、参数和完整单元测试所在的主项目；
2. `~/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01`：把算法移植到公司 `px4ctrl` RTL 插件接口后的实机插件源码。

修改第一套代码不会自动更新第二套代码。只在 `~/fanhang` 修改并通过仿真，不代表机载的 `.so` 已包含修改；只修改插件而不回写主项目，也会造成两个版本逐渐分叉，给后续排错和 AI 交接带来困难。

推荐始终执行以下闭环：

```text
明确需求和版本基线
        ↓
修改 ~/fanhang 算法、配置、测试和说明
        ↓
运行主项目单元测试 / RViz 仿真
        ↓
按接口能力同步到 manual_return_v01 插件源码
        ↓
更新插件测试、版本号、日志和 README
        ↓
使用老板指定 Docker 镜像编译和测试
        ↓
覆盖正式 manual_return_v01.so 并核验哈希/依赖
        ↓
机载备份、替换、加载确认、低风险实飞
        ↓
保存日志、哈希、结论并更新交接记录
```

## 2. 当前目录和文件职责

### 2.1 仿真/算法主项目

工作区：

```text
/home/tobeki/fanhang
```

主要文件：

| 文件 | 职责 |
|---|---|
| `src/manual_return_planner/include/manual_return_planner/manual_return_planner.h` | 算法配置、输入输出数据结构、规划器声明 |
| `src/manual_return_planner/src/manual_return_planner.cpp` | 轨迹预处理、Reverse、RDP、航点生成等核心逻辑 |
| `src/manual_return_planner/include/manual_return_planner/safe_rdp.h` | Safe-RDP 接口和数据结构 |
| `src/manual_return_planner/src/safe_rdp.cpp` | 点云安全校验逻辑 |
| `src/manual_return_planner/src/manual_return_node.cpp` | ROS 参数加载、历史轨迹记录、规划调用和执行衔接 |
| `src/manual_return_planner/src/command_gate_node.cpp` | 控制权和命令门控；不属于纯规划插件逻辑 |
| `src/manual_return_planner/config/manual_return.yaml` | ROS/仿真配置 |
| `src/manual_return_planner/test/` | 主项目单元测试 |
| `src/manual_return_planner/README.md` | 算法、参数、使用方法和限制说明 |

### 2.2 老板提供的发布包

当前正式发布目录：

```text
/home/tobeki/lidar_ws/src/px4ctrl_release
```

关键内容：

| 路径 | 职责 |
|---|---|
| `px4ctrl_node` | 老板提供的主控可执行程序，通常不得由我们重新编译或擅自修改 |
| `include/plugin_interface.h` | 插件 ABI 和 RTL 接口定义，编译时必须以当前包里的版本为准 |
| `plugins/mission_rtl_plugin/` | 老板提供的 Mission RTL 插件；与当前普通 RTL 插件是两条接口 |
| `plugins/manual_return_v01/` | 我们的普通 RTL 插件源码 |
| `plugins/manual_return_v01.so` | 最终部署到无人机的插件产物 |
| `README.md` | 老板发布包的使用说明 |

老板原始压缩包应当保留，不要直接在压缩包里覆盖文件：

```text
/home/tobeki/lidar_ws/src/px4ctrl_release_20260826.tar.gz
```

### 2.3 插件源码

```text
/home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01
```

| 文件 | 职责 |
|---|---|
| `manual_return_v01.h` | 插件版算法配置、统计结构和规划入口 |
| `manual_return_v01.cpp` | 从主项目移植并适配 NED/插件接口后的核心算法 |
| `rtl_plugin.cpp` | `PluginBase` 包装、插件注册、版本、日志和 `plugin_state` |
| `test_manual_return_v01.cpp` | 插件算法测试，不依赖 ROS |
| `test_plugin_load.cpp` | 使用 `dlopen` 验证导出接口、元数据、ABI 和 RTL 冒烟运行 |
| `CMakeLists.txt` | 独立编译 `.so` 和测试程序 |
| `README.md` | 当前算法来源、构建方法、插件版本和 API 限制 |

### 2.4 构建记录和备份

```text
/home/tobeki/lidar_ws/build_records
```

每次修改正式插件之前，都应在这里建立带日期、版本或用途的备份目录。不要只保留一个名字相同的旧 `.so`，否则无法确认无人机上运行的是哪一版。

## 3. 当前已知版本快照

以下信息表示 2026-08-27 wholeMap 捷径回退源码完成后的快照。下一次开始工作时必须重新执行命令确认，不能只相信本文档：

```text
V0.1 算法历史基线：f741627eaeaa2371148fa2eb6948a69eb8ef5e59
老板 px4ctrl 源版本：df1183d
插件源码版本：0.1.4-f741627-mapfallback（尚未生成 .so）
当前正式 .so 版本：0.1.2-f741627-landhandoff
插件功能：普通 RTL；不实现 Mission RTL
```

当前正式文件哈希：

```text
px4ctrl_node:
0a6549b94f2fd320e66b142854fe0f2cf972df640a28885cba3b84a0326fdd8e

manual_return_v01.so:
bcdfe31be5a03b8a8a35dae57b57f11b8f71529bc3d53e0d5ecd72c9f66af8e5

完整旧版备份：
/home/tobeki/lidar_ws/src/px4ctrl_release_backup_bcdfe31_20260827_V1.1
/home/tobeki/lidar_ws/src/px4ctrl_release_backup_ffe8e2d_20260827_V1.2
```

开始新任务时重新记录：

```bash
cd /home/tobeki/fanhang
git rev-parse HEAD
git status --short

sha256sum \
  /home/tobeki/lidar_ws/src/px4ctrl_release/px4ctrl_node \
  /home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01.so

docker image inspect \
  registry.boundlessi.com/uavob/core/gx-navi:ci \
  --format '{{.Id}} {{.Architecture}}/{{.Os}} {{json .RepoDigests}}'
```

`docker image inspect` 只读取本机镜像身份，不会修改代码，也不要求每次 WSL 重启后重新登录。只有凭据失效、执行 `docker pull` 被拒绝或本机尚无该镜像时，才重新执行：

```bash
docker login registry.boundlessi.com
docker pull registry.boundlessi.com/uavob/core/gx-navi:ci
```

不要把账号、密码或访问令牌写进本文档、源码、日志或 AI 对话交接文件。

## 4. 修改前必须完成的检查

### 4.1 明确本轮改动属于哪一层

先判断需求属于以下哪一种：

1. **纯路径算法**：预处理、Reverse、RDP、Safe-RDP、Home 端点、yaw、最大线段长度等；通常可以同步进 `.so`。
2. **算法参数**：参数默认值、阈值、安全半径等；可以同步，但插件当前主要使用编译期默认值，不会自动读取 `manual_return.yaml`。
3. **ROS/执行器行为**：轨迹记录频率、控制权切换、速度/加速度执行、到点停顿、降落状态机等；通常不在当前插件 API 能力内。
4. **插件包装和诊断**：版本、优先级、日志、`plugin_state`；只修改插件侧。
5. **px4ctrl 接口或主控行为**：需要老板提供新接口或新 `px4ctrl_node`，不能假设只改 `.so` 就能完成。

如果需求属于第 3 或第 5 类，应先检查 `plugin_interface.h` 是否提供了所需数据和控制入口。当前普通 RTL API 只提供：

```text
flownTraj：NED、按时间正序排列的 x/y/z/yaw
wholeMap：ENU/world 点云
rtlTraj：插件输出的 NED 返航航点
```

当前已确认 `wholeMap` 的来源语义：NEWMAP / CONTINUOUS_MAP / FUSING 下是
DLO 累计的 `dlo_map`，LOCATE 下是预加载的离线地图。它是障碍表面点云，
不是带“已知自由/未知”状态的占据栅格。因此当前策略只用它检查 RDP 新增的
非相邻历史点捷径；碰撞捷径恢复原历史区间，不直接执行 A*，也不通过返回
`false` 假装取消返航（宿主可能转而使用其他插件或内置 RTL）。

此外，`PluginBase::onInit(FSMData*)` 提供主控运行时上下文。当前插件只额外读取
`FSMData::arm_pos` 和 `param->takeoff_land.land_target_height`，用来生成与 Land
状态连续的低空交接点。访问这些字段时必须使用当前老板发布包的完整头文件重新
编译，不能把旧包的结构布局用于新 `px4ctrl_node`。

它不提供：

- 每个样本的时间戳；
- 速度和加速度；
- RTL 精确触发时的实时位姿；
- 轨迹执行器的到点策略；
- 强制悬停而禁止主控回退内置 RTL 的错误状态。

因此，这些功能不能只靠移植 `~/fanhang` 中的 ROS 代码来实现。

### 4.2 检查未提交修改

```bash
cd /home/tobeki/fanhang
git status --short
git diff --check
git diff
```

已有修改默认属于当前用户，不得用 `git reset --hard`、`git checkout --` 或删除目录来“清理”。AI 接手时必须先理解现有差异。

### 4.3 阅读当前接口和说明

至少阅读：

```text
~/lidar_ws/src/px4ctrl_release/README.md
~/lidar_ws/src/px4ctrl_release/include/plugin_interface.h
~/lidar_ws/src/px4ctrl_release/plugins/README.md（如存在）
~/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01/README.md
~/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01/CMakeLists.txt
```

新收到老板的发布包时，还应先查看压缩包内容，不要立即覆盖现有目录：

```bash
tar -tzf /home/tobeki/lidar_ws/src/px4ctrl_release_YYYYMMDD.tar.gz | less
```

### 4.4 备份插件和正式产物

示例：

```bash
backup_dir=/home/tobeki/lidar_ws/build_records/manual_return_v01_pre_change_YYYYMMDD_HHMM
mkdir -p "$backup_dir"

cp -a \
  /home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01/. \
  "$backup_dir/"

cp -a \
  /home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01.so \
  "$backup_dir/manual_return_v01.so"
```

备份后至少记录旧 `.so` 的哈希：

```bash
sha256sum "$backup_dir/manual_return_v01.so"
```

## 5. 代码同步矩阵

这是以后修改时最重要的检查表。

| 修改内容 | `~/fanhang` 需要同步 | 插件目录需要同步 | 额外动作 |
|---|---|---|---|
| 轨迹预处理、Reverse、RDP、端点逻辑 | `manual_return_planner.h/.cpp` | `manual_return_v01.h/.cpp` | 两边都增加相同语义的测试 |
| Safe-RDP、点云碰撞检查 | `safe_rdp.h/.cpp` 及主规划器 | 通常合并在 `manual_return_v01.cpp` | 保持 ENU→NED 转换和安全半径一致 |
| 算法参数新增/删除/默认值变化 | Config 结构、`manual_return.yaml`、`manual_return_node.cpp`、README | `manual_return_v01.h` 的 `Config`、校验代码、README | 插件不自动读取 ROS YAML，必须明确参数来源 |
| Home/降落交接规则 | 主规划器、配置、README、回归测试 | `manual_return_v01.cpp`、`rtl_plugin.cpp`、CMake、README、回归测试 | 使用物理 arm 点和 `land_target_height`，不要直接采用约 1 m 高的 `flownTraj.front()` |
| yaw 计算 | 主规划器及测试 | `manual_return_v01.cpp` 及测试 | 主项目 ENU 与插件 NED 的公式不能盲目照抄 |
| 状态枚举/错误处理 | 主项目状态和测试 | `Status`、`PlanStats`、日志、测试 | 插件失败返回 `false` 后主控可能回退内置 RTL |
| 插件版本、名称、优先级、日志 | 一般无需修改 | `rtl_plugin.cpp` | 同步更新 `test_plugin_load.cpp` 和插件 README |
| ROS 轨迹记录、控制门、执行速度、到点停顿 | ROS node/配置/测试 | 当前通常无法同步 | 先确认老板 API；可能需要新主控或新接口 |
| Mission RTL | 主项目视需求 | 当前 `manual_return_v01` 不应实现 | 由老板独立的 Mission RTL plugin 负责 |

### 5.1 为什么不能直接复制主项目 `.cpp`

两边的数据模型和坐标系不同：

- `~/fanhang` 的仿真/ROS 规划主要使用 ENU/world 语义；
- `flownTraj` 和 `rtlTraj` 是 NED；
- `wholeMap` 仍然是 ENU/world，插件内部必须转换后再做安全检查；
- 主项目包含时间戳、速度检查和 ROS 执行信息，插件 API 没有这些字段；
- 主项目的 ROS node 负责执行衔接，插件只负责生成航点数组。

所以同步的目标是“保持算法语义一致”，而不是逐行复制。

### 5.2 推荐的同步顺序

1. 在 `~/fanhang` 修改核心代码。
2. 同时修改主项目配置、README 和单元测试。
3. 主项目测试通过后，列出本轮真正改变的算法行为。
4. 在插件 `manual_return_v01.h/.cpp` 中按 NED 和 API 限制重做同一行为。
5. 在 `test_manual_return_v01.cpp` 增加对应的插件回归测试。
6. 更新 `rtl_plugin.cpp` 的版本号、启动日志和必要的 `plugin_state` 字段。
7. 更新 `test_plugin_load.cpp` 的预期版本。
8. 更新插件 README 中的算法步骤、版本、限制和构建基线。
9. 搜索旧参数、旧版本和废弃函数，确认没有遗漏。

示例搜索：

```bash
rg -n \
  '旧函数名|旧参数名|旧版本号' \
  /home/tobeki/fanhang/src/manual_return_planner \
  /home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01
```

## 6. 主项目修改和验证

### 6.1 修改要求

每个行为修复至少应包含：

- 核心实现修改；
- 能复现旧问题的回归测试；
- 配置项同步；
- README 同步；
- `git diff --check` 无格式错误。

例如降落交接点修复不能只替换一个 z 值，还要增加“输入首点是约 1 m 高的悬停 Home 时，输出末点仍为低空 landing handoff”的测试，防止以后再次引入同类问题。

### 6.2 编译主项目包

```bash
cd /home/tobeki/fanhang
source /opt/ros/noetic/setup.bash

catkin_make \
  --pkg manual_return_planner \
  -DCATKIN_ENABLE_TESTING=ON \
  -DCMAKE_BUILD_TYPE=Release
```

### 6.3 运行测试

运行整个包的测试：

```bash
cd /home/tobeki/fanhang
source /opt/ros/noetic/setup.bash

catkin_make run_tests_manual_return_planner
catkin_test_results build/test_results/manual_return_planner
```

只运行核心返航测试：

```bash
catkin_make run_tests_manual_return_planner_gtest_test_manual_return
catkin_test_results build/test_results/manual_return_planner
```

验收条件：

- 构建命令退出码为 `0`；
- `catkin_test_results` 显示 `0 errors`、`0 failures`；
- 新增回归测试确实被执行，而不只是“旧测试通过”；
- `git diff --check` 无输出。

根据改动风险，再运行相应 RViz 仿真。算法测试通过不能替代实机前的坐标系、端点和执行行为检查。

## 7. 插件版本管理

行为发生变化时，应更新 `rtl_plugin.cpp` 中 `DECLARE_PLUGIN_EX` 的版本。例如：

```text
0.1.0-f741627
0.1.1-f741627-homefix
0.1.2-f741627-landhandoff
```

同时更新：

1. `rtl_plugin.cpp` 的初始化日志；
2. `rtl_plugin.cpp` 的 `toJson()` 诊断字段；
3. `test_plugin_load.cpp` 的预期版本；
4. 插件 README 的 `list_plugins` 预期结果。

版本名要能区分实机运行的不同算法，不能每次都生成同名、同版本但内容不同的 `.so`。此外应使用 SHA256 精确确认二进制身份。

当前插件只实现普通 RTL：

```cpp
int rtlTrajApiVersion() const override {
    return RTL_PLUGIN_API_VERSION;
}
```

不要在没有明确需求和测试的情况下增加 `missionRtlTrajApiVersion()`。其默认值 `0` 能确保老板的独立 Mission RTL 插件继续负责 Mission 返航。

## 8. 使用 Docker 编译插件

### 8.1 为什么必须用老板的镜像

`.so` 与 `px4ctrl_node` 之间存在 C++ ABI、编译器、PCL 和系统动态库依赖。即使在 WSL 本机能够编译，也不能证明机载主控可以安全加载。因此正式产物必须使用：

```text
registry.boundlessi.com/uavob/core/gx-navi:ci
```

如果老板发布了新 `px4ctrl_release`，必须使用新包里的 `plugin_interface.h` 重新编译插件，不能直接复用旧 `.so`。

### 8.2 启动开发容器

WSL Docker 集成正常时：

```bash
docker run -it --rm \
  --name gxnavi-dev \
  -u "$(id -u):$(id -g)" \
  -v "$HOME/lidar_ws:/home/ramy/lidar_ws" \
  -w /home/ramy/lidar_ws \
  registry.boundlessi.com/uavob/core/gx-navi:ci \
  bash
```

说明：

- `--rm` 表示退出容器后删除容器本身；挂载在宿主机 `~/lidar_ws` 中的源码和构建产物不会删除；
- 下次进入通常重新执行 `docker run`；
- `-u` 避免构建产物变成 root 所有；
- 出现 `I have no name!` 或 `cannot find name for group ID 1000` 一般只是镜像中没有 UID/GID 对应名称，只要 `id`、挂载目录和写权限正常即可；
- 不要把 Markdown 链接形式的镜像地址复制进命令，镜像名必须是纯文本。

如果需要保留容器，去掉 `--rm`，以后使用：

```bash
docker start -ai gxnavi-dev
```

### 8.3 Docker Desktop 的 WSL 集成不可用时

当前电脑曾出现 WSL 中 `/usr/bin/docker` 不可用、但 Windows Docker Desktop 正常的情况。可以直接调用：

```text
/mnt/c/Users/15518/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe
```

使用 WSL UNC 路径挂载发布目录的示例：

```bash
/mnt/c/Users/15518/AppData/Local/Programs/DockerDesktop/resources/bin/docker.exe \
  run --rm \
  --mount 'type=bind,source=\\wsl.localhost\Ubuntu-20.04\home\tobeki\lidar_ws\src\px4ctrl_release,target=/release' \
  -w /release/plugins/manual_return_v01 \
  registry.boundlessi.com/uavob/core/gx-navi:ci \
  bash
```

如果 WSL 发行版名称、Windows 用户名或 Docker Desktop 安装路径变化，需要相应修改，不能原样照抄。

### 8.4 容器内编译和测试

进入容器后：

```bash
cd /home/ramy/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01

cmake \
  -S . \
  -B build_manual_return_v01 \
  -DCMAKE_BUILD_TYPE=Release \
  -DPX4CTRL_RELEASE_INCLUDE_DIR=../../include

cmake --build build_manual_return_v01 -j"$(nproc)"

cd build_manual_return_v01
ctest --output-on-failure
```

正式候选产物：

```text
plugins/manual_return_v01/build_manual_return_v01/manual_return_v01.so
```

如果使用 `/release` 作为容器挂载点，则把第一条 `cd` 改为：

```bash
cd /release/plugins/manual_return_v01
```

插件是独立 CMake 工程，正常情况下不需要对整个 `lidar_ws` 执行 `catkin_make`。只有老板明确要求重新构建完整工作区时，才使用全工作区构建流程。

## 9. 替换正式 `.so` 并做最终核验

测试通过后，先确认源文件和目标文件：

```text
源：plugins/manual_return_v01/build_manual_return_v01/manual_return_v01.so
目标：plugins/manual_return_v01.so
```

在宿主机执行：

```bash
cd /home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01

install -m 755 \
  build_manual_return_v01/manual_return_v01.so \
  ../manual_return_v01.so
```

然后检查：

```bash
sha256sum \
  build_manual_return_v01/manual_return_v01.so \
  ../manual_return_v01.so \
  ../../px4ctrl_node

cmp \
  build_manual_return_v01/manual_return_v01.so \
  ../manual_return_v01.so

file ../manual_return_v01.so

nm -D --defined-only ../manual_return_v01.so | \
  rg 'plugin_(create|destroy|get_info|get_api_version)'

ldd ../manual_return_v01.so | rg 'not found' || true
```

判断方式：

- 两个 `.so` 的 SHA256 必须相同；
- `cmp` 退出码必须为 `0`；
- 当前目标应为 `ELF 64-bit LSB shared object, x86-64`；
- 必须存在 `plugin_create`、`plugin_destroy` 等导出符号；
- `ldd` 不应出现 `not found`；
- `px4ctrl_node` 的哈希必须与修改前记录一致。

还应使用老板镜像直接加载正式路径中的 `.so`，而不只是加载 build 目录里的候选文件：

```bash
cd /home/ramy/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01
./build_manual_return_v01/test_plugin_load ../manual_return_v01.so
```

预期最后出现类似：

```text
Plugin export, metadata, RTL API, Mission RTL isolation, and RTL smoke test passed
```

## 10. 收到老板新发布包时的处理流程

老板明确说“放弃原来的、使用整个新包”时，不能在旧发布目录上只挑几个文件拼接。推荐流程：

1. 保存老板原始压缩包和 SHA256；
2. 用 `tar -tzf` 查看结构；
3. 解压到新的临时目录，不直接覆盖当前正式目录；
4. 阅读新包 README 和插件 README；
5. 记录新 `px4ctrl_node` 哈希、版本和 `plugin_interface.h` 哈希；
6. 比较 RTL/Mission RTL API 常量、虚函数和数据结构；
7. 把我们的插件源码移入新包对应的 `plugins/manual_return_v01/`；
8. 使用新包的头文件重新编译并运行全部插件测试；
9. 把新 `.so` 放入新包的 `plugins/` 目录；
10. 验证老板附带的其他插件仍在，并确保我们的普通 RTL 插件不抢占 Mission RTL；
11. 整包通过后，再把它标记为新的正式发布目录。

每个新发布包都应视为新的 ABI 基线。即使接口宏版本号表面没变，也建议重新编译和动态加载测试。

## 11. 机载部署流程

机载实际目录由无人机配置决定，目前不能在没有证据时硬编码。应通过负责无人机的同事、进程启动参数、配置文件或 `list_plugins` 确认。

### 11.1 部署前

- 无人机必须处于 DISARM；
- 桨叶安全、场地和飞手安排符合公司流程；
- 记录机载旧 `px4ctrl_node` 和旧插件的哈希；
- 备份机载旧 `.so`；
- 确认本轮是“只替换 `.so`”还是老板同时要求覆盖 `px4ctrl_node`；
- 如果老板要求使用他的新主控，必须使用与该主控同一发布包头文件编译的 `.so`。

### 11.2 替换后

1. 设置 `.so` 为可读、可执行；
2. 按公司批准的方式重启 `px4ctrl_node`；
3. 使用 WebSocket `list_plugins` 页面检查插件；
4. 确认名称、版本、状态和优先级；
5. 检查启动日志是否包含本轮独有的版本标识；
6. 在不飞行时先触发可执行的加载/状态检查；
7. 再做低高度、短直线测试；
8. 通过后做带一个直角拐弯的短路径测试；
9. 保存完整日志、路径、现象和测试条件。

当前降落交接点修复版应显示：

```text
name: manual_return_v01
version: 0.1.2-f741627-landhandoff
state: running
```

启动日志应包含：

```text
[ManualReturnV01] initialized (algorithm f741627+landhandoff, px4ctrl binary df1183d)
```

### 11.3 出现异常时

不要立即连续实飞。先保存以下信息：

- `list_plugins` 输出；
- `px4ctrl_node` 和 `.so` 的 SHA256；
- 插件启动日志和 `plugin_state`；
- RTL 触发前后的状态；
- 输入点数、预处理点数、输出点数；
- `status`、最大 RDP 偏差、地图可用性、最小净空和不安全线段数；
- 飞行高度、路径长度、转弯形状和异常发生位置；
- 是否由普通 `rtl` 还是 Mission 中的 RTL 触发。

返航路径生成正常但“每隔一段停一下”时，不应直接认定是 RDP 问题。还要检查主控逐航点执行、到点阈值、速度规划和控制状态机；这些可能不在当前插件内。

## 12. 常见错误和判断方法

### 12.1 `catkin_make` 成功但没有 `px4ctrl_node`

如果 `find src -name package.xml` 没找到任何包，`catkin_make` 可能只创建了一个空工作区，退出码仍为 `0`。必须先确认老板开发包已经正确解压到 `lidar_ws/src`。

当前 `manual_return_v01` 是独立插件工程，应使用它自己的 CMake 命令生成 `.so`，而不是用空工作区的 `catkin_make` 判断插件已经完成。

### 12.2 `I have no name!`

这是使用宿主机 UID/GID 进入镜像，而镜像 `/etc/passwd` 中没有对应用户名造成的提示。只要：

```bash
id
pwd
touch /home/ramy/lidar_ws/.write_test
```

均正常，通常不影响编译。测试后可删除 `.write_test`。

### 12.3 `.so` 可以编译但主控加载失败

重点检查：

- 是否使用当前老板发布包的 `plugin_interface.h`；
- 是否使用老板指定的 Docker 镜像；
- 插件架构是否与机载主控一致；
- `ldd` 是否缺库；
- 是否导出 `plugin_create`、`plugin_destroy` 等符号；
- `RTL_PLUGIN_API_VERSION` 是否匹配；
- 机载加载的是否真的是新 `.so`，用 SHA256 而不是文件名判断；
- 文件权限、插件目录和配置路径是否正确；
- 是否存在同名或更高优先级的插件。

### 12.4 修改了 YAML，但实机行为没变化

`manual_return.yaml` 属于 ROS 主项目。当前 `.so` 中的 `Config` 使用编译时默认值，除非插件包装明确接入参数系统，否则修改 YAML 不会自动改变机载插件。

### 12.5 普通 RTL 和 Mission RTL 混淆

WebSocket 直接发送 `rtl` 使用普通 RTL 路径；Mission 任务内部触发的返航可能使用 Mission RTL。当前 `manual_return_v01` 只覆盖普通 RTL。测试记录必须写清触发方式。

## 13. 每轮完成后的交付清单

代码层：

- [ ] `~/fanhang` 核心代码已修改；
- [ ] 主项目配置、README 和测试已同步；
- [ ] 插件 `manual_return_v01.h/.cpp` 已同步；
- [ ] 坐标系和插件 API 限制已单独检查；
- [ ] 插件版本、日志、`plugin_state`、加载测试预期已同步；
- [ ] 插件 README 已更新；
- [ ] 没有误改老板的 `px4ctrl_node` 和其他插件。

验证层：

- [ ] 主项目 Release 构建通过；
- [ ] 主项目相关测试通过；
- [ ] Docker 内插件编译通过；
- [ ] 插件算法测试通过；
- [ ] 插件动态加载测试通过；
- [ ] 正式 `.so` 与 build 产物完全一致；
- [ ] `ldd` 无 `not found`；
- [ ] 导出符号正确；
- [ ] 新旧 SHA256 已记录；
- [ ] 老板 `px4ctrl_node` 哈希未发生非预期变化。

实机层：

- [ ] 机载旧文件已备份；
- [ ] `list_plugins` 能看到正确版本且状态正常；
- [ ] 日志确认调用了我们的插件；
- [ ] DISARM 状态下完成静态检查；
- [ ] 短直线低风险测试完成；
- [ ] 短直角路径测试完成；
- [ ] 日志、哈希、现象、结论已保存。

## 14. 多 AI 窗口交接模板

新开 AI 窗口时，建议把下面内容连同本工作流文档一起提供。不要只说“继续改插件”。

```text
项目目标：
- 当前只做 V0.1 实机普通 RTL 插件，不进行 V0.2 路径优化（如仍成立）。

本轮需求：
- [具体描述要修改的行为]

明确禁止：
- 是否允许修改代码；
- 是否允许修改老板的 px4ctrl_node；
- 是否允许部署或实飞。

关键目录：
- 主项目：/home/tobeki/fanhang
- 正式发布包：/home/tobeki/lidar_ws/src/px4ctrl_release
- 插件源码：/home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01
- 正式插件：/home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01.so
- 构建记录：/home/tobeki/lidar_ws/build_records

版本基线：
- fanhang git HEAD：
- fanhang git status：
- 算法来源提交：
- 老板 px4ctrl 源版本：
- Docker image ID/RepoDigest：
- plugin_interface.h SHA256：
- px4ctrl_node SHA256：
- 当前插件版本和 SHA256：

已完成修改：
- [逐文件说明]

已完成测试：
- [命令、通过数量、失败数量]

当前产物：
- [绝对路径、大小、SHA256]

已知限制/未解决问题：
- [例如 API 无时间戳、速度、精确触发位姿]

下一步：
- [唯一明确的下一动作]
```

可用以下命令快速采集交接状态：

```bash
cd /home/tobeki/fanhang
git rev-parse HEAD
git status --short
git diff --stat

sha256sum \
  /home/tobeki/lidar_ws/src/px4ctrl_release/include/plugin_interface.h \
  /home/tobeki/lidar_ws/src/px4ctrl_release/px4ctrl_node \
  /home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01.so

stat -c '%n | %s bytes | mode %a | %y' \
  /home/tobeki/lidar_ws/src/px4ctrl_release/plugins/manual_return_v01.so
```

## 15. AI 执行本项目时的最低要求

接手的 AI 应当：

1. 先阅读本文档、项目交接文档、主项目 README、发布包 README 和插件 README；
2. 区分用户当前请求与文档中的历史说明，不把历史步骤当成新的授权；
3. 修改前检查 Git 状态和正式文件哈希；
4. 不覆盖用户未提交的修改；
5. 不擅自修改或重编老板的 `px4ctrl_node`；
6. 不把 ENU 主项目代码逐行复制进 NED 插件；
7. 每个行为修改都补充主项目和插件两侧测试；
8. 必须在老板 Docker 镜像里构建和动态加载验证；
9. 最终报告修改文件、测试结果、产物路径、版本和 SHA256；
10. 对无法由插件 API 实现的需求明确说明，不伪造接口或假设机载路径。

只要严格执行以上闭环，就能在多轮修改、多个 AI 窗口和多次老板发布包更新之间，保持算法代码、插件源码、二进制产物和实机测试结论可追踪。
